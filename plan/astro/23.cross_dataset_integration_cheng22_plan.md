# Plan: Cross-Dataset Astrocyte Integration (cheng22 only) — Harmony + 10 PCs (Scripts 24 & 25)

## Context
Same approach as scripts 22/23 but restricted to cheng22 (P28, P38) only — yoo25 is excluded. gao25 P56 remains the reference for HVG selection, PCA fitting, Harmony correction, and kNN label transfer.

---

## Key Challenge: Different Gene Sets
- gao25: 32,285 genes; cheng22: 16,572 genes (a shared subset)
- Must intersect gene sets, reselect HVG from common genes, refit PCA on gao25 P56 cells

---

## Script 24: `24.cross_dataset_integration_cheng22.py`

### File Paths (top of script, capitalized)
```
INPUT_GAO25       = links/astro/gao25_scrna_astro.h5ad
INPUT_CHENG22     = links/astro/cheng22_astro.h5ad
LABELS_P56_IN     = local_data/res/astro/17.labels_P56.parquet
RES_DIR           = local_data/res/astro/
PARQUET_COMBINED  = local_data/res/astro/24.combined_labels.parquet
PARQUET_VERTICES  = local_data/res/astro/24.archetype_vertices.parquet
```

### Parameters
```python
N_TOP_GENES  = 2000
NDIM_PCA     = 10
K_NEIGHBORS  = 15
CHENG22_AGES = ['P28', 'P28_dr', 'P38', 'P38_dr']
```

### Steps

1. **Load reference (gao25 P56)**
   - Filter to `Age == 'P56'`; load `17.labels_P56.parquet`; verify count match

2. **Load cheng22**
   - Filter `obs['Age']` to `CHENG22_AGES`; fail if any age missing

3. **Find common genes + HVG selection**
   - `common_genes = sorted(set(adata_gao25.var_names) & set(adata_cheng22.var_names))` (full gao25)
   - Subset both to common genes
   - `select_hvg()` from `scripts/common.py` on gao25 P56 (common genes only)

4. **Normalize** (`norm()` from `scomics.utils`)
   - Independently per dataset: CP10k → log2(1+x) → z-score per gene

5. **Fit PCA on P56, project all**
   ```python
   pca = PCA(n_components=NDIM_PCA)
   pca.fit(xn_p56)
   xp_p56_raw     = pca.transform(xn_p56)
   xp_cheng22_raw = pca.transform(xn_cheng22)
   pc_cols = [f'PC{i+1}' for i in range(NDIM_PCA)]   # PC1..PC10
   ```

6. **Harmony batch correction**
   ```python
   import harmonypy as hm
   n_p56, n_cheng22 = len(xp_p56_raw), len(xp_cheng22_raw)
   xp_all_raw   = np.vstack([xp_p56_raw, xp_cheng22_raw])
   batch_labels = ['gao25'] * n_p56 + ['cheng22'] * n_cheng22
   meta_df      = pd.DataFrame({'batch': batch_labels})
   ho           = hm.run_harmony(xp_all_raw, meta_df, 'batch', random_state=42)
   xp_all_h     = ho.Z_corr            # harmonypy 2.x returns (ncells, ndim) directly
   xp_p56       = xp_all_h[:n_p56]
   xp_cheng22   = xp_all_h[n_p56:]
   ```

7. **kNN label transfer** (on harmony-corrected embeddings)
   ```python
   knn = KNeighborsClassifier(n_neighbors=K_NEIGHBORS)
   knn.fit(xp_p56, arch_labels_p56)
   labels_cheng22 = knn.predict(xp_cheng22)
   ```

8. **Joint UMAP** on `xp_all_h`

9. **Archetype centroid vertices** — mean of `xp_p56` rows per archetype label (harmony-corrected)

10. **Save combined parquet**
    - Columns: `PC1..PC10, UMAP1, UMAP2, dataset, age, donor_name, cell_type, archetype`
    - `donor_name`: gao25 from `obs['donor_name']`; cheng22 from `obs['Sample']`
    - `cell_type`: gao25 gets `''`; cheng22 from `obs['Type']`

---

## Script 25: `25.cross_dataset_integration_cheng22_viz.py`

### File Paths (top of script, capitalized)
```
PARQUET_COMBINED_IN   = local_data/res/astro/24.combined_labels.parquet
ARCHETYPE_VERTICES_IN = local_data/res/astro/24.archetype_vertices.parquet
FIG_DIR               = local_data/fig/astro/
HTML_PER_DATASET      = local_data/fig/astro/25.scatter_{dataset}.html
HTML_ALL_DATASETS     = local_data/fig/astro/25.scatter_all_datasets.html
HTML_UMAP_ALL         = local_data/fig/astro/25.umap_all_datasets.html
HTML_BARPLOT          = local_data/fig/astro/25.archetype_abundance.html
```

### Parameters
```python
PANELS      = [(0, 1, 'PC1', 'PC2'), (0, 2, 'PC1', 'PC3'), (1, 2, 'PC2', 'PC3')]
PANEL_3D    = (0, 1, 2, 'PC1', 'PC2', 'PC3')
UMAP_PANELS = [(0, 1, 'UMAP1', 'UMAP2')]
DATASETS    = ['gao25', 'cheng22']
```

### Plots
1. Per-dataset PCA scatter — colored by archetype (one HTML per dataset)
2. All-datasets PCA scatter — colored by archetype, dataset, or age
3. All-datasets UMAP scatter — colored by archetype, dataset, or age
4. Archetype abundance barplot — fraction per (dataset, age) group, natsorted

---

## Critical Existing Functions to Reuse
- `select_hvg()` — `scripts/common.py`
- `norm()` — `src/scomics/utils.py`
- `scatter_categorical_html()` — `scripts/viz.py`
- `stacked_bar_html()` — `scripts/viz.py`
- `KNeighborsClassifier`, `PCA` — sklearn
- `harmonypy.run_harmony` — harmonypy

---

## Verification
```bash
conda run -n archetype python scripts/astro/24.cross_dataset_integration_cheng22.py
# Expect: harmony convergence log, shapes for P56+cheng22, UMAP done, parquets saved

conda run -n archetype python scripts/astro/25.cross_dataset_integration_cheng22_viz.py
# Open 25.umap_all_datasets.html: 2 datasets, archetype clusters visible
# Open 25.archetype_abundance.html: bars across P28/P38/P56 ages
```
